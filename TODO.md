## План развития MonoFocus Planner

> **Важно**: Все задачи реализуются **порционно**, с тестированием после каждого шага. Избегаем наложения нескольких слоёв логики в одном коммите.

---

### 1. PWA (Progressive Web App)

**Цель**: получить ощущение нативного приложения без адресной строки и UI браузера, удобный запуск с главного экрана телефона.

**Промпт для реализации**:
```
Реализовать PWA для MonoFocus Planner:
1. Создать manifest.json с иконками, названием, цветами темы (Android Chrome)
2. Настроить vite для генерации и подключения manifest
3. Добавить service worker для кэширования статики (минимальный офлайн: приложение открывается без сети, данные в localStorage)
4. Настроить display: standalone для открытия без адресной строки
5. Протестировать установку на Android Chrome и поведение при обновлениях версии

Важно: реализовать пошагово, тестировать после каждого шага. Не смешивать manifest, SW и display в одном коммите.
```

**Основные шаги**:
- Добавить `manifest.json` с иконками, названием, цветами темы (Android Chrome).
- Настроить `vite` / сборку на генерацию и подключение manifest.
- Добавить service worker для кэширования статики (минимальный офлайн: приложение открывается даже без сети, данные всё так же в `localStorage`).
- Настроить корректный `display: standalone`, чтобы приложение открывалось без адресной строки.
- Протестировать установку на Android Chrome и поведение при обновлениях версии (обновление SW, кэша).

---

### 2. Режим фокуса в Today — запуск из задачи

**Текущее поведение**:
- Одна всегда видимая кнопка фокуса в форме добавления задач (слева внизу).
- Фокус запускается только на первую задачу списка Today.
- После паузы или возврата задачи из Done активная задача часто уезжает в конец списка, что ломает логику «фокус на верхнем элементе».

**Промпт для реализации**:
```
Переработать режим фокуса в Today:
1. Убрать кнопку старта фокуса из формы добавления задач (внизу Today)
2. Добавить кнопку запуска фокуса в развёрнутом виде каждой задачи (справа, рядом с Edit; Edit сдвинуть в центр)
3. Убедиться, что после паузы задача остаётся на своём месте (taskOrderByDay не меняется из-за фокуса)
4. Удалить старую логику «фокус всегда запускается для верхней задачи» (проверить, что нет скрытых зависимостей)
5. Привязать запуск/паузу/продолжение к конкретной задаче по id, а не по позиции в списке

Важно: реализовать пошагово. Сначала убрать старую кнопку и проверить, что ничего не сломалось. Потом добавить новую кнопку в развёрнутом виде. Не трогать логику паузы/продолжения до проверки базового функционала.
```

**Желаемое поведение**:
1. После паузы режима фокуса задача **остаётся на своём месте** в текущем порядке (`taskOrderByDay` не меняется из-за фокуса).
2. Кнопка старта фокуса **убирается** из формы добавления задач (внизу Today).
3. В **развёрнутом виде каждой задачи** во вкладке Today появляется кнопка запуска фокуса:
   - расположение: справа, рядом с текущей кнопкой `Edit` (кнопку `Edit` сдвинуть в центр, кнопку фокуса — вправо);
   - поведение: запуск/пауза/продолжение привязаны к **конкретной задаче**, а не к позиции в списке.
4. Старую логику «фокус всегда запускается для верхней задачи» аккуратно удалить, чтобы не было скрытых зависимостей.

**Детали реализации**:
- В сторе уже есть `activeTaskId` и `activeTaskStartedAt`; нужно убедиться, что выбор активной задачи идёт по `id`, а не по индексу в списке.
- При возврате задачи из Done в Today порядок определяется только логикой undone и не должен зависеть от фокуса.

---

### 3. Быстрое добавление задач по дням недели (Week View)

**Промпт для реализации**:
```
Добавить быстрое создание задач по дням недели в Week:
1. Добавить кнопку "+" слева от счётчика задач в каждом дне недели
2. При нажатии открыть модальное окно (bottom-sheet на мобильном, центрированное на десктопе)
3. В модалке: заголовок (обязательный), чекбокс 🐸, кнопки Cancel/Add
4. После подтверждения: создать задачу со статусом todo, plan.day = выбранный день, plan.week = getWeekString(day), добавить в конец taskOrderByDay[day]

Важно: использовать существующий паттерн модалок (как Move в Week). Не трогать существующую логику добавления задач в бакет. Тестировать на мобильном и десктопе отдельно.
```

**Идея**: на каждом дне недели в `Week` добавить компактную кнопку `+` слева от счётчика задач.

**Поведение**:
- При нажатии на `+` открывается модальное окно создания задачи **для конкретного дня** (того, где нажали кнопку).
- В модалке минимум полей:
  - заголовок задачи (обязательный текст),
  - чекбокс `🐸` (лягушка) в том же стиле, что в других формах,
  - кнопки «Cancel» / «Add».
- После подтверждения:
  - создаётся новая задача со статусом `todo`,
  - `plan.day` = выбранный день,
  - `plan.week` = неделя, соответствующая этому дню (`getWeekString(day)`),
  - задача добавляется в конец порядка дня (учитывая `taskOrderByDay[day]`).

**Детали UX**:
- На мобильном модалка может быть bottom‑sheet; на десктопе — центрированное небольшое окно.
- При создании задачи день автоматически разворачивается, если он был свернут.

---

### 4. Сортировка в бакете и днях недели (Week View)

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Реализовать строго по шагам, тестируя после каждого. Избегать наложения логики.

**Промпт для реализации (ШАГ 1 — только бакет, только десктоп)**:
```
Добавить dnd-сортировку в бакете недели (только десктоп, без мобильного):
1. Использовать @dnd-kit (как в Today), НЕ нативный HTML5 drag&drop
2. Добавить в AppState поле taskOrderByWeekBucket: Record<string, string[]> (weekStr -> ordered task IDs)
3. Обернуть список weekTasks в SortableContext и DndContext
4. Создать обёрточный компонент SortableBucketTaskItem (использует useSortable, прокидывает transform/style в BucketTaskItem)
5. При dnd внутри бакета обновлять taskOrderByWeekBucket[currentWeek]
6. Применить сохранённый порядок при рендере (как в Today с taskOrderByDay)

Важно: НЕ трогать логику переноса задач между днями/бакетом. НЕ трогать дни. Только бакет, только десктоп. Тестировать: сортировка работает, порядок сохраняется после перезагрузки, Edit/Move/Delete/Done не сломались.
```

**Промпт для реализации (ШАГ 2 — бакет на мобильном)**:
```
Включить dnd-сортировку в бакете на мобильном:
1. Добавить TouchSensor в sensors (как в Today)
2. Протестировать на реальном телефоне: долгий тап, скролл + перетаскивание
3. Убедиться, что не конфликтует с существующими тапами (Edit/Move/Delete/Done)

Важно: если на мобильном возникают конфликты, оставить только Move-модалку для переноса, dnd только на десктопе.
```

**Промпт для реализации (ШАГ 3 — дни недели)**:
```
Добавить dnd-сортировку внутри дней недели:
1. Для каждого дня обернуть список задач в SortableContext и DndContext
2. Создать обёрточный компонент SortableDayTaskItem (использует useSortable, прокидывает transform/style в DayTaskItem)
3. При dnd внутри дня обновлять taskOrderByDay[day] (как в Today)
4. Синхронизировать порядок: если день наступил, Today — источник правды; если день в будущем, Week может устанавливать порядок

Важно: НЕ трогать логику переноса между днями/бакетом. Только сортировка внутри дня. Тестировать: сортировка работает, порядок синхронизируется с Today для текущего дня, Edit/Move/Delete/Done не сломались.
```

**Промпт для реализации (ШАГ 4 — опционально, dnd между днями/бакетом)**:
```
Добавить dnd-перетаскивание задач между днями и бакетом (опционально):
1. Реализовать drop zones для каждого дня и бакета
2. При drop обновлять plan.day/plan.week и порядок в целевом контейнере
3. Сохранить Move-модалку как альтернативу на мобильном

Важно: это опциональный шаг. Если возникают конфликты с существующей логикой переноса, оставить только Move-модалку.
```

**Текущее состояние**:
- В `Today` есть dnd‑сортировка, порядок хранится в `taskOrderByDay[today]` и синхронизируется с соответствующим днём во `Week`.
- В `Week` реализован перенос задач между бакетом и днями (через Move‑модал), но **нет сортировки** внутри:
  - бакета недели (задачи без `plan.day`),
  - будущих дней недели.

**Желаемое поведение**:
- Возможность менять порядок задач **в бакете недели** dnd‑перетаскиванием.
- Возможность менять порядок задач **в каждом дне недели** (включая будущие дни), dnd‑перетаскиванием прямо во `Week`.
- Порядок в `Week` для конкретного дня должен быть единым с `Today` для этого дня (если день наступил, Today — источник правды / или аккуратная синхронизация).

**Ключевые принципы реализации** (из опыта багов):
- ✅ **Использовать только @dnd-kit**, НЕ нативный HTML5 drag&drop (он конфликтует с touch на мобильных).
- ✅ **Один источник правды**: для дней — `taskOrderByDay[day]`, для бакета — `taskOrderByWeekBucket[weekStr]`.
- ✅ **Минимизировать пересечения логики**: реализовать сортировку отдельно, не трогая перенос задач, скрытие прошлых дней, edit/expand.
- ✅ **Унифицированные компоненты**: dnd накручивать снаружи (SortableContext + обёрточный компонент с useSortable), не меняя внутренности DayTaskItem/BucketTaskItem.
- ✅ **Порционная стратегия**: каждый шаг — отдельный мини‑релиз с минимальным количеством затронутого кода.
- ✅ **Тестовая матрица**: Desktop Chrome, мобильный Chrome (реальные тапы, долгий тап, скролл + перетаскивание), перезагрузка страницы.

---

### 5. Inbox — унификация и улучшения

**Промпт для реализации (ШАГ 1 — визуальная унификация)**:
```
Унифицировать визуальный стиль Inbox с остальными вкладками:
1. Привести карточки ProcessItem к единому стилю: text-sm для основного текста, text-xs для мета, p-3, border, rounded-lg, shadow-sm
2. Исправить перенос длинного текста (break-all для строк без пробелов)
3. Проверить, что кнопки не ломаются при длинном тексте

Важно: только визуальные изменения, не трогать логику обработки захватов. Тестировать на мобильном и десктопе.
```

**Промпт для реализации (ШАГ 2 — проверка синхронизации)**:
```
Проверить и исправить синхронизацию Inbox с Week View:
1. Проверить, что выбранная неделя и день корректно синхронизируются с Week View (особенно для будущих недель)
2. Убедиться, что задачи из Inbox попадают в правильный день/неделю

Важно: только проверка и исправление багов, не добавлять новую функциональность.
```

**Визуальное поведение**:
- Привести карточки Inbox к тому же стилю, что в Today/Week/Done/Events:
  - единый размер шрифтов (`text-sm` для основного текста, `text-xs` для мета),
  - единые паддинги и бордеры (`p-3`, `border`, `rounded-lg`, `shadow-sm`),
  - правильный перенос длинного текста (включая строки без пробелов) без ломания кнопок.

**Поведение захвата → задача**:
- Уже есть выбор: в Today или в определённую неделю (через селектор недели).
- Оставить эту механику, но:
  - аккуратно проверить, что выбранная неделя и день корректно синхронизируются с Week View (особенно для будущих недель).

**Идеи на будущее (пока не реализовывать)**:
- Пресеты типов задач (личное/работа/идея и т.д.).
- Более «умный» выбор времени/недели (выходные, через 2 недели и т.п.).
- Свайпы на мобильном:
  - свайп влево: отправить в week‑bucket текущей недели,
  - свайп вправо: отправить в Today.
- Эти идеи зафиксировать, но реализовывать после базовой унификации UI и сортировки.

---

### 6. Статистика и дашборд

**Промпт для реализации (ШАГ 1 — слой вычислений)**:
```
Создать слой вычислений статистики:
1. Создать файл utils/statistics.ts с чистыми функциями
2. Функции на вход получают массив задач, возвращают агрегаты (количество, время, лягушки по периодам)
3. Функции: getTodayStats, getWeekStats, getMonthStats, getYearStats, getAllTimeStats
4. Протестировать функции на тестовых данных

Важно: только вычисления, без UI. Не трогать существующие вкладки.
```

**Промпт для реализации (ШАГ 2 — базовая вкладка Statistics)**:
```
Создать базовую вкладку Statistics:
1. Добавить вкладку Statistics в навигацию
2. Отобразить базовые блоки: Сегодня / Эта неделя / Этот месяц / Этот год (цифры и суммарное время)
3. Использовать функции из utils/statistics.ts
4. Добавить фильтры: Неделя / Месяц / Год / Всё время

Важно: только базовые цифры, без графиков. Тестировать корректность вычислений.
```

**Промпт для реализации (ШАГ 3 — графики)**:
```
Добавить графики в Statistics:
1. Выбрать лёгкую библиотеку для графиков (или простой SVG/Canvas-рендер)
2. Добавить линейные/столбчатые графики по времени/количеству задач
3. Добавить блок по лягушкам: сколько запланировано/сделано за период

Важно: не тянуть тяжёлые зависимости. Протестировать производительность на мобильном.
```

**Имеющиеся данные**:
- Даты планирования и выполнения (`plan.day`, `plan.week`, `updatedAt`),
- статус задач (`todo`/`done`),
- флаг `frog`,
- `timeSpent` для завершённых задач,
- история за все дни/недели/месяцы.

**Цели статистики**:
- Показать динамику продуктивности:
  - количество выполненных задач по дням/неделям/месяцам/годам,
  - суммарное время по периодам,
  - отдельная статистика по «лягушкам»,
  - streak'и (серии дней/недель с выполненными задачами) — опционально.

**Форма представления**:
- Отдельная вкладка **Statistics** или скрытый раздел, доступный из `Settings` (например, кнопка «Open dashboard»).
- Дашборд с несколькими блоками:
  - «Сегодня / Эта неделя / Этот месяц / Этот год» — короткие цифры и суммарное время.
  - Графики по времени/количеству задач (линейные/столбчатые).
  - Блок по лягушкам: сколько «лягушек» запланировано/сделано за период.
- Фильтры вида: **Неделя / Месяц / Год / Всё время**.

**Реализация (в общих чертах)**:
- Завести слой вычислений статистики (чистые функции, которые на вход получают массив задач и возвращают агрегаты).
- Использовать эти агрегаты во вьюхе Statistics, не перегружая `Done` и другие основные вкладки.
- Для графиков выбрать лёгкую библиотеку или сделать простой SVG/Canvas‑рендер, чтобы не тянуть тяжёлые зависимости.

---

### 7. Синхронизация между устройствами (отложено)

**Статус**: отложено на будущее после стабилизации функционала и UI.

**Условия для старта работ**:
- Приложение «устаканилось»: PWA, сортировки, Inbox, статистика протестированы и удобны.
- Появилась реальная потребность использовать Планер на нескольких устройствах (телефон + ноутбук и т.п.).

**Предварительные мысли (на будущее)**:
- Нужен бэкенд или облачное хранилище (Firebase/Supabase/свой API) + авторизация.
- Необходимо решить вопросы конфликтов (одновременное изменение с разных устройств), версионирования и миграций.
- PWA и текущая архитектура (SPA + `localStorage`) хорошо лягут на офлайн‑first синхронизацию, но это отдельный крупный этап.

---

## Чеклист перед каждым коммитом

- [ ] Проверено на десктопе (Chrome)
- [ ] Проверено на мобильном (реальный телефон, Android Chrome)
- [ ] Нет дублирующих компонентов в одном файле
- [ ] Нет неиспользуемых импортов/переменных
- [ ] Не смешаны несколько слоёв логики в одном изменении
- [ ] Изменения минимальны и изолированы
- [ ] Существующий функционал не затронут (или явно задокументировано, почему)
